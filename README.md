# social-friend-recommendation-bfs
This project implements a social media friend recommendation system using the Breadth-First Search (BFS) algorithm in C. It models users as nodes in a graph and their friendships as edges using an adjacency matrix. The system suggests new friends based on mutual connections (level-2 relationships).
